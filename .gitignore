node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Raw Playwright captures; the transcoded copy lives in public/
recordings

# Console clips are served from the CDN; local copies are render inputs only
public/galileo-*.mp4
